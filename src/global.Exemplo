const host = 'localhost'

export default host;

// crie um arquivo global.js na pasta 'src' e cole esse codigo acima ,para rodar no expo go precisa mudar o host para seu ip, 
// e tbm no web precisa rodar o comando: 'php artisan serve --host=0.0.0.0 --port=8000' 
// ,e se eu n me engano precisa baixar uma versão antiga do expo go pq a atual n funfa com nosso app