import { StyleSheet } from "react-native";

export default StyleSheet.create({
  
    container: {
        flex: 1,
        backgroundColor: '#FFFFFF',
        overflow: 'scroll',
        backgroundColor: 'red',
        alignItems: 'center',
      },
      inputs:{
        width: '100%',
        height: 50,
        borderWidth: 1,
        borderColor: '#000000',
        borderRadius: 10,

      }
});